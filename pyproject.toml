[tool.poetry]
name = "cmdlib"
version = "0.3.0"
description = ""
authors = ["Xavier Martinez-Hidalgo <xavier@martinezhidalgo.org>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest-cov = "^2.8"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
